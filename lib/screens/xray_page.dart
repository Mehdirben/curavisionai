import 'package:flutter/material.dart';

class XRayPage extends StatelessWidget {
  const XRayPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('X-Ray Analysis'),
    );
  }
}